import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)
import java.awt.*;

/**
 * Write a description of class TopBottomWall here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class RightWall extends Wall
{
    /**
     * Act - do whatever the TopBottomWall wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {}
    public RightWall()
    {
        /*GreenfootImage wall = new GreenfootImage(SQUARE_SIZE, WALL_SIZE);
        wall.setColor(Color.darkGray);
        wall.fillRect(0,0, SQUARE_SIZE, WALL_SIZE);
        this.setImage(wall);*/
    }
}
